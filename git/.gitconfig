[user]
	email = abijith.b@keyvalue.systems
	name = Abijith B

[include]
    path = /home/abijith/themes.gitconfig

[core]
    pager = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true    # use n and N to move between diff sections
    light = false      # set to true if you're in a terminal w/ a light background color (e.g. the default macOS terminal)
    side-by-side = true
    line-numbers = true
    features = collared-trogen

[merge]
    conflictstyle = diff3

[diff]
    colorMoved = default

